const STATUS_WORKSPACE = ['open', 'close'];
const ROLES = ['Admin', 'Manager', 'Staff'];
const STATUS_REQUEST = ['Approved', 'Rejected'];

module.exports = { STATUS_WORKSPACE, ROLES, STATUS_REQUEST };
